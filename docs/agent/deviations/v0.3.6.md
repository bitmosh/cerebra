# Deviations — v0.3.6 (Phase 9 Steps 1–2)

## DEV-025 — ClutchStubEngine audit findings (pre-rename state)

**Audit findings before any Phase 9 changes:**

- File: `cerebra/cognition/clutch_stub.py` (109 lines)
- Class: `ClutchStubEngine` — fully stateless; no state accumulated between `decide()` calls
- `ClutchContext` fields: `step_index`, `step_count`, `composite_score`, `last_clutch_action`, `total_steps_run` — NO `evaluation`, `outcome`, `cycle_state`, or `cycle_config` fields
- `ClutchDecision` fields: `action`, `rule_matched`, `escalate_to_catalyst` (always False) — NO `cascade_depth`
- `BUILTIN_PREDICATES`: 6 predicates (`at_terminal_step`, `composite_below_threshold`, `composite_above_threshold`, `first_step`, `step_index_at`, `always`)
- Engine behavior: first-match-wins cascade; no cascade tracking; default fallback to `accept` / `default_no_match` on no match
- No `escalate_to_catalyst=True` path; it was dead code (field exists, always False)
- Consumers: `cerebra/cognition/cycle_runtime.py` (import + two `ClutchContext` construction sites); `tests/unit/test_clutch_stub.py` (28 tests)
- `CycleConfig` had NO `composite_floor` field — floor was unused/implicit

**Rename actions taken:** `clutch_stub.py` → `clutch.py`, `ClutchStubEngine` → `ClutchEngine`, `test_clutch_stub.py` → `test_clutch.py`. All imports updated in one pass.

---

## DEV-026 — cascade_depth field added to ClutchDecision and ClutchDecisionMade event payload

**What changed:** `ClutchDecision` gained `cascade_depth: int = 0` (default 0, backward compat). `ClutchDecisionMade` event payload now includes `cascade_depth` and `escalate_to_catalyst` fields. Two construction sites in `cycle_runtime.py` updated.

**Why:** Observability. Without cascade_depth, there's no way to know whether a cycle accepted quickly (first rule) or deep in the cascade (fifth rule). This matters for catalyst escalation logic in Step 2 and for debugging rule ordering.

**Impact on fossic vocabulary:** `ClutchDecisionMade` event shape extended. New fields are additive (existing consumers that don't read them are unaffected). `docs/aseptic/cross-pollination/pass-9.1.md` notifies fossic of the vocabulary extension.

**Scope:** Non-breaking. Default value (0) means any code reading old event payloads without the field will get a missing-key error if it assumes the field exists, but no current consumer reads `cascade_depth` from stored events.

---

## DEV-027 — composite_floor field added to CycleConfig (non-breaking, default 0.3)

**What changed:** `CycleConfig` dataclass gained `composite_floor: float = 0.3`. `_parse_config` reads it from YAML via `data.get("composite_floor", 0.3)`. `CycleRuntime` reads `self.config.composite_floor` for per-step `consecutive_steps_below_floor` tracking.

**Why:** The `consecutive_steps_below_floor` predicate needs a reference floor per cycle. Making it a cycle-level config field allows per-cycle tuning (e.g., critical cycles use 0.5; exploratory cycles use 0.2). The stop condition `composite_floor_consecutive` continues to use its own `threshold` parameter — two independent concepts with coincidentally similar semantics.

**Impact:** All existing cycle configs that lack `composite_floor` continue to parse and run without modification. `simple.planning.v0.yaml` was not modified. Default value (0.3) matches the value previously hardcoded in stop condition parameters.

---

## DEV-028 — catalyst_was_invoked ships as always-False placeholder

**What changed:** `catalyst_was_invoked` predicate added to `BUILTIN_PREDICATES` and `BUILTIN_PREDICATE_NAMES`. Always returns `False` in Phase 9 Step 1.

**Why:** Adding the predicate name to the registry now allows cycle configs written for Phase 9 Step 2 to reference it today. Cycle configs are validated against `BUILTIN_PREDICATE_NAMES` at load time; if the name isn't registered, loading fails. By shipping the name in Step 1, Step 2 only needs to wire the implementation — no config-layer changes required.

**Impact:** Any cycle config referencing `catalyst_was_invoked` will load successfully but the predicate will always evaluate to False until Step 2 ships the real implementation.

---

## DEV-029 — ClutchContext new fields all optional for backward compat

**What changed:** `ClutchContext` gained four new fields: `evaluation: EvaluationPacket | None = None`, `outcome: OutcomeRecord | None = None`, `cycle_state: ClutchCycleState = field(default_factory=ClutchCycleState)`, `cycle_config: CycleConfig | None = None`. All have defaults.

**Why:** 28 existing tests in `test_clutch.py` construct `ClutchContext` directly with keyword arguments matching the original 5-field schema. Making all new fields optional preserves those tests without modification.

**Impact:** The 7 new state-aware predicates gracefully return `False` when their required context field is `None`. This means predicates like `prediction_severe_miss` can be safely registered even in engine configurations where `outcome` is not available (e.g., LLM failure paths in `cycle_runtime.py`).

---

## DEV-031 — Primitive structure audit findings (Phase 9 Step 2 pre-implementation)

**Audit scope:** `cerebra/_primitives/` — all six existing primitives plus `__init__.py` and `VENDORED_FROM.md`.

**Patterns confirmed:**
- Module-level docstring: 3–5 lines, explains purpose + composition formula or pattern. No path-comment header (the `# lattica/_primitives/filename.py` comment in the spec code blocks is an annotation for the doc, not production code).
- Imports: stdlib only, no external deps. `from __future__ import annotations` not used in existing primitives (Python 3.12+, native union syntax is fine).
- Structure: dataclasses before class definitions; class before any free functions.
- Docstrings: class-level (one line or brief), method-level using Args/Returns for non-obvious signatures.
- `_primitives/__init__.py`: alphabetical per-module imports, `__all__` list. No sub-packages.
- `cognition/__init__.py`: bulk import from `cerebra._primitives` in one block, `__all__` list with per-category grouping.
- `VENDORED_FROM.md`: records source, date, and canonical-copy discipline. Updated in this pass to record the bandit addition.
- Python target: 3.12+ (pyproject.toml `requires-python = ">=3.12"`). `X | None` union syntax valid throughout.
- No existing test files import `Bandit` or `BanditSelection`.

**No deviations from existing pattern in the bandit implementation.**

---

## DEV-032 — Bandit implementation: RNG is stored but not yet used in v0.1

**What:** `Bandit.__init__` accepts `rng: random.Random | None = None` and stores it as `self._rng`. Per the §11 spec, the RNG is used only for tiebreaking, which is "not implemented in v0.1 — first-wins for ties is acceptable." The `select` method does not call `self._rng` in any code path.

**Why recorded:** The injected-RNG pattern is correct and necessary for future weighted-random selection (Step 3's Catalyst). The field exists at the right level. The current select implementation is deterministic without the RNG because: (1) forced_exploration always picks the first unseen arm by declaration order, and (2) UCB selection uses strict `>` comparison so ties resolve by first-encounter order without randomness.

The determinism test (`test_same_seed_same_operations_produce_identical_state`) still passes because the full operation sequence (update → select → update → select) produces identical state through pure order-preservation, not through seeded randomness.

**Impact on consumers:** Step 3 (CatalystEngine) will use `get_stats()` to read arm statistics rather than calling `select()` directly. The RNG will become relevant if/when the Catalyst or a future consumer needs weighted-random selection among UCB-equivalent arms.

---

## DEV-030 — cross-pollination file convention adopted (first use)

**What changed:** `docs/aseptic/cross-pollination/` directory created. `pass-9.1.md` is the first cross-pollination file.

**Why:** `ClutchDecisionMade` event payload extension (`cascade_depth`, `escalate_to_catalyst` now documented as emitted fields) affects the canonical `AGENT_TRACE_VOCABULARY.md` that fossic owns. Aseptic discipline requires notifying the other project. Cerebra has not previously produced a cross-pollination file.

**Convention adopted:** File naming follows `pass-N.M.md` (matching fossic's pattern from prior cross-pollination artifacts). Directory is `docs/aseptic/cross-pollination/` under the Cerebra repo root. This is the canonical location for all future cross-pollination files.

---

## DEV-033 — Phase 9 Step 3 pre-implementation clarifications (Q1-Q3 resolutions)

**Audit findings and resolutions applied before Phase 9 Step 3 implementation:**

- **Q1 (role field on CycleStep):** Added `role: str = ""` to the `CycleStep` dataclass with empty-string default for backward compat. All existing cycle configs (and any configs that omit `role`) continue to parse without modification. `planning.adaptive.v0` populates `role` for all five steps.
- **Q2 (floor param on consecutive_steps_below_floor predicate):** The `floor` parameter in the planning research doc was decorative. The predicate reads the floor from `cycle_state.consecutive_steps_below_floor` (which CycleRuntime tracks using `config.composite_floor`); the predicate parameters contain only `count`. Removed `floor: 0.3` from the predicate params in `planning.adaptive.v0.yaml`.
- **Q3 (refine_plan template context):** Used `{{ prior_step_output }}` (prior accepted output as a single string) plus `{% if strategy_hint %}` block. No `prior_steps[]` indexed lookback. `render_template` scope unchanged except for the addition of if-block support and `strategy_hint` key.

**Implementation adds:** `{% if var %}...{% endif %}` single-level if-block support to `render_template` via `_process_if_blocks` inner function using `re.DOTALL`. No new dependencies required (Jinja2 remains uninstalled per security constraints).

---

## DEV-034 — Research doc mismatches corrected pre-implementation

**What changed:** Two mismatches between the Phase 9 Step 3 research docs and the actual implementation:

1. **`Bandit.get_stats()` API:** Research docs implied a dict return and no-arg call. Actual API is `get_stats(arm_id: str) -> ArmStats`. Fixed: `{a.arm_id: self._bandit.get_stats(a.arm_id) for a in self._arms}`.
2. **`Bandit.from_state()` args:** Research docs implied `from_state(state, arm_list)`. Actual signature is `from_state(state: dict, rng=None)`. Fixed: pass pre-built state dict `{"exploration_weight": 1.4, "arms": arms_data}` with no arm list.

**Why recorded:** Both mismatches were caught by runtime errors in early test runs (not by audit). Research docs were correct about intent but wrong about call signature details.

---

## DEV-035 — Catalyst reward not recorded on LLM failure steps

**What changed:** When an LLM call fails (both retries fail), CycleRuntime resets `pending_catalyst_selection = None` and `pending_strategy_hint = ""` before entering the Clutch path. This means the bandit does NOT receive a reward update for that step.

**Why:** LLM failures produce composite=0.0 by convention, but that 0.0 doesn't reflect actual quality of the catalyst arm's strategy — it reflects infrastructure failure. Training the bandit on these synthetic 0.0 rewards would unfairly penalize whatever arm was selected. The bandit learns only from steps where the LLM actually ran and the evaluator scored the output.

**Impact:** Arms selected before an LLM failure remain "sampled" by the bandit's count only if `record_reward` was already called for them (it wasn't). So after an LLM failure, the arm that was in `pending_catalyst_selection` before the failure will be re-explored next time as an unsampled arm (count=0). This is a minor behavioral quirk but is preferable to poisoned reward data.
